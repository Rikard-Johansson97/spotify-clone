import React, { FC } from "react";
import { Box, Avatar, Typography } from "@mui/material";
interface SongProps {}

const Song: FC<SongProps> = ({}) => {
  return <div>Song</div>;
};

export default Song;
